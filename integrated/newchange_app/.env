# ── Database (Attendeez) ─────────────────────────────────────────────────────
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=db_attendance
DB_PORT=3306

# ── SSO ──────────────────────────────────────────────────────────────────────
# Must match the Portal's app.secret_key EXACTLY so the JWT is readable
# by both apps when doing local-decode (optional fallback).
SECRET_KEY=super_secret_key

# URL of the Portal — used by Main/sso.py to call /api/verify-token
PORTAL_URL=http://127.0.0.1:5000

# ── Email / SMTP (Attendeez notifications) ──────────────────────────────────
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=xzonnazarenoguinto@gmail.com
SMTP_PASSWORD=uucy ihdt vwza oxvm

# ── Flask ────────────────────────────────────────────────────────────────────
FLASK_DEBUG=True
